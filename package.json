{
    "devDependencies": {
        "vitepress": "^1.3.3"
    },
    "scripts": {
        "docs:dev": "vitepress dev docs",
        "docs:build": "vitepress build docs",
        "docs:preview": "vitepress preview docs --port 3000",
        "start": "npm run docs:build &&  npm run docs:preview"
    }
}
