{
    "dependencies": {
        "vitepress": "^1.3.3"
    },
    "scripts": {
        "docs:dev": "vitepress dev docs",
        "docs:build": "vitepress build docs",
        "docs:preview": "vitepress preview docs",
        "start": "npm run docs:preview -- --port $PORT",
        "build": "npm run docs:build"
    }
}
