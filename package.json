{
    "dependencies": {
        "vitepress": "^1.3.3"
    },
    "scripts": {
        "docs:build": "vitepress build docs",
        "docs:preview": "vitepress preview docs",
        "start": "npm run docs:preview -- --port 3000",
        "build": "npm run docs:build"
    }
}
